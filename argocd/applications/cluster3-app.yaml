apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: store-cluster3
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/avatala/kubernetes-cd
    targetRevision: main
    path: kustomize-apps/store/overlays/dev/cluster3

  destination:
    server: https://10.40.0.2
    namespace: store

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true